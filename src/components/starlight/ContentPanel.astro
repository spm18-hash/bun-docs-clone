<div class="content-panel">
    <div class="sl-container"><slot /></div>
</div>

<style>
    @layer starlight.core {
        .content-panel:has(+ .content-panel) {
            padding-block: 0 1.25rem;
        }
        .content-panel {
            & + .content-panel {
                padding: 1.25rem 0;
            }

            &:has(.not-found) {
                padding: 12rem 0;
            }

            @media (min-width: 75rem) {
                padding: 1.25rem 0;
            }
        }

        .sl-container > :global(* + *) {
            margin-top: 1.5rem;
        }

        .sl-container:has(.not-found) {
            width: fit-content;
        }

        @media (min-width: 72rem) {
            .sl-container {
                margin-inline: var(--sl-content-margin-inline, auto);
            }
        }
    }
</style>
